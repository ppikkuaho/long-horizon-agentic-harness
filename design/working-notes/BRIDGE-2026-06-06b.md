# Bridge — L1-L5 Agent Harness (2026-06-06, late) — Phase 6 behavioural validation in flight

> Continuity across a compaction. Project: build the **L1-L5 agent harness** (5-level LLM-agent system
> on a pinned Claude Code that builds software from user intent). All work is in
> `~/Documents/l1-l5-agent-harness/`. **Read first:** `design/INDEX.md`, this bridge, then
> `harnessd/IMPLEMENTATION-PLAN.md` + `design/BEHAVIOURAL-VALIDATION.md`. Supersedes `BRIDGE-2026-06-06.md`.

## WHERE WE ARE — substrate DONE; now validating per-level + review BEHAVIOUR (Phase 6)

**Phase 5 substrate (Inc 0–17) + Inc 18 (pieces-present): COMPLETE + committed + mutation-verified.**
**OAuth + security jail + deterministic-trust: built, wired, live-verified.** **Suite: 578 passed + 1
skipped (real_boot)** (+ env-gated skips). HEAD ≈ `668e5d7`.

The work now is **Phase 6 — behavioural validation** (`design/BEHAVIOURAL-VALIDATION.md`): does each
level DO what it's meant to + FOLLOW its workflow, and do the up-the-tree REVIEWS/GATES work. This is the
"trace-through" — pour real intent through, find leaks (a leak = the flow silently routing AROUND a gap).

### Key infra built this session (all committed)
- **OAuth RESOLVED:** the pinned install has its OWN `claude auth login` (in `.cc-pinned/config`); the
  binary SELF-AUTHS. **The pinned token expires ~daily** → real-agent evals need a fresh login: run
  `! …/.cc-pinned/.../claude.exe auth login` (browser opens, user clicks Authorize, binary auto-captures).
  Daily-login AUTOMATION is deferred (needs reliable browser-click; superpowers CDP flaked).
- **Security jail** (`harnessd/spawn/sandbox.py`): real sandbox-exec seatbelt, gate-1 live-verified.
  `resolve_containment`/`render_profile`/`wrap`. WIRED into the adapter on an explicit per-spawn
  containment request (FORK-JAIL-TRIGGER). **Eval self-auth tier:** drop the keychain mach-deny + the
  Library/Keychains read-deny so the binary self-auths (trusted box); write-jail + other secret-denies hold.
- **Deterministic-trust** (`harnessd/spawn/cc_config.py::seed_trust`): pre-seeds `.claude.json`/`settings.json`
  so a spawned agent boots dialog-free (no trust dialog, no bypass warning, no per-tool prompts). The
  adapter adds `--dangerously-skip-permissions` ONLY when jailed (the jail is the bound).
- **THE EVAL HARNESS** (`harnessd/eval/interactive_eval.py`): spawn a REAL jailed agent (self-auth,
  dialog-free) + score behaviour. Two transports: **interactive tmux** (`run_interactive_eval` — for L1's
  multi-turn intake; makes freezes visible) + **autonomous `-p`** (`run_autonomous_eval` — for L2-L5
  artifact-producers; wall-clock timeout = freeze guard) + `run_jailed` (the execute-review primitive).
  The **counterpart-simulator** (`counterpart_reply`) plays the human/parent (scenario-brief-driven, with a
  WITHHOLD rule for leak tests). **Decoupled-level approach (user-directed):** don't run the full upstream
  cascade to test a level — `generate_synthetic` makes a clean upstream artifact (the L1 output / intent-spec)
  one-shot, feed it to the real jailed level agent. Per-level eval scripts in `tools/eval_l{1..5}_*.py`.

## THE BUILD LOOP (carry this — for both substrate increments AND eval findings)
Substrate increments: test-first Workflow (tester→builder→2 reviews) → MY verification gate (re-run +
**mutation-test** load-bearing properties) → commit. Eval findings: run the real agent → review (often a
multi-agent Workflow) → if a DESIGN gap, diagnose ROOT CAUSE → fix the docs (per llm-design-principles) →
RE-RUN to verify the fix closes → commit. I ground truth myself (grep/read), never trust a subagent's
"verified". Forks: decide the most spec-faithful option, implement, document in `FORK-DECISIONS.md`,
keep moving (standing goal — don't block on a nod; present at finished build, task #6).

## PER-LEVEL EVAL RESULTS SO FAR (the heart of this session)
- **L1 intake — STRONGLY POSITIVE** (`dev/eval-runs/l1-intake-complete.json`). Real jailed L1: read its 9
  role docs first, outcomes-first questions, calibrated ask-vs-assume, **leak test PASSED** (escalated on
  under-spec, didn't invent). The interactive-tmux run validated it. (One-shot transport has a checkbox-
  widget fragility — L1 is the most human-tuned level, so "it works" is enough; don't over-invest.)
- **L2 architect — MOSTLY PASS + a FIND→FIX→VERIFY loop (the standout).** 10-agent review Workflow
  (`dev/eval-runs/l2-review/`). Leak test PASS (deferred the open conflict policy, firewalled the decided
  online-CRDT from it), output-contract/MNF/in-lane PASS, M49 workflow PARTIAL, **trace-blocks FAIL**.
  → **D-EVAL-1 FIXED + VERIFIED:** the docs told L2 to tag each area `kind:requirement` dotted-under-one-
  parent — IMPOSSIBLE for a multi-intent area. Fix (**FORK-TRACE-AREA**): an area is a `kind:decision`
  carving record (`DD-NNN`, `serves:[intents]`); obligations inside get clean single-parent dotted
  requirements. Edited `operational/L2/role.md` + `design/PLAN-ALIGNMENT-GATE.md` (+ worked area example,
  closed field set, truncation self-check). **RE-RAN → trace-block FAIL now CLEAN** (areas=decision, no
  DR-A*, 27/27 truncation-consistent, leak test didn't regress, agent restated the rule). D-EVAL-2 (M49
  pattern gap — substrate-first/walking-skeleton absent) recorded, NOT yet fixed.
- **L3 design — BOTH LEAK TESTS PASSED** (`dev/eval-runs/l3-design.json`). Pressure-tested the planted
  broken Search Port (renegotiated upward), escalated the cross-area Change-Event dependency, AND
  independently corroborated the trace-block parent-anchoring gap (escalated it, didn't silently invent).
- **L4 coordinate — RUNNING NOW** (`tools/eval_l4_coordinate.py`, bg task at compaction). Tests the
  test-first discipline (frozen acceptance tests from a SEPARATE tester lateral BEFORE any L5) + escalate-
  don't-absorb on a planted under-framed workstream. **On resume: check `dev/eval-runs/l4-coordinate.json`
  + the `.eval-tmp/l4eval-*/plan.md`; review vs the L4 rubric in BEHAVIOURAL-VALIDATION.md.**
- **L5/L5+ — script READY, not yet run** (`tools/eval_l5_execute.py`). L5 executes a real coding task vs
  frozen tests; **L5+ reviews code with a KNOWN planted defect** (passes the frozen tests but violates spec
  R-4 comment-skipping) → must CATCH+BOUNCE (the reliable "reviewer-works" test, per the user).

## USER DIRECTIVES (live, must honor)
1. **"No dummy agents"** — evals use REAL models (agent-under-test + counterpart-sim + reviewers). The
   per-level evals burn real OAuth usage (approved).
2. **For L5: ALSO check the L5+ reviewer works** (done in the script — planted-defect test).
3. **Then a FULL minimal-feature run (the markdown→PDF tool) — whose REAL purpose is verifying the
   up-the-tree REVIEWS/GATES** work: L5+ reviews L5, L4 reviews the L5+ REPORT (not raw code), L3 reviews
   workstream integration, L2 compatibility review, the plan-alignment gate. Small feature so the cascade
   COMPLETES. Approach: a MANUAL trace-through (chain real level + review runs) until the live daemon
   cascade is commissioned; plant a defect at a boundary and confirm the review above bounces. (Captured
   in BEHAVIOURAL-VALIDATION.md "The full trace-through".)
4. **DEFERRED (future quality pass):** intentionally DESIGN the per-level METHODOLOGY/process (not just the
   role description) — give each level a structured plan→review→adversarial loop (Workflows or custom).
   The L2 single-shot-but-gappy result is the evidence. (In DEFERRED-REGISTER.md.)
5. **The L2 doc fix was user-approved + collaborative** — for further role-doc changes, the design fork is
   the user's call (P1/P11); diagnose + recommend, implement on approval (I have a standing nod for clear
   fixes but present the reasoning).

## NEXT STEPS (in order)
1. **Bring L4 home** — check the run output, review `plan.md` vs the L4 rubric (test-first discipline +
   escalate-scope leak tests), record the result + any design finding (fix-loop if a doc gap).
2. **Run L5/L5+** (`python3 tools/eval_l5_execute.py`) — confirm L5 executes + **L5+ catches the planted
   defect + bounces.** (Needs a fresh pinned token if expired — `claude auth login`.)
3. **The full minimal-feature trace-through (markdown→PDF)** — the review-machinery test (directive #3).
   Build it as a manual chain (`tools/eval_full_trace.py` or similar): L1 intake (counterpart-sim) → L2 →
   L3 → L4 (+tester lateral) → L5/L5+, firing each REVIEW as a real run, planting a defect at a boundary,
   confirming the review above bounces.
4. **Fix D-EVAL-2** (M49 substrate-first/walking-skeleton emphasis) if pursuing the L2-finding loop.
5. Eventually: present the accumulated FORK decisions (task #6) at finished build.

## KEY FILES
- `design/BEHAVIOURAL-VALIDATION.md` — the Phase-6 framing + ALL per-level rubrics (L1–L5/L5+) + the
  full-trace/review-machinery plan.
- `design/working-notes/DEFERRED-REGISTER.md` — Lessons 1–8, the OAuth/jail/eval findings, D-EVAL-1
  (VERIFIED FIXED) + D-EVAL-2, the per-level-methodology deferral, the L1/L2/L3 eval results.
- `design/working-notes/FORK-DECISIONS.md` — all build forks incl. FORK-TRACE-AREA, FORK-JAIL-TRIGGER.
- `harnessd/eval/interactive_eval.py` + `tools/eval_l*.py` — the eval instrument + per-level scripts.
- `dev/eval-runs/` (gitignored) — the captured eval transcripts/artifacts/verdicts.
- `harnessd/IMPLEMENTATION-PLAN.md` — the increment queue (0-17 substrate done; 18-24 Phase 6) + BUILD-STATUS banner.

## RUNNING A REAL EVAL (mechanics)
- Fresh token first if expired: `! $PWD/.cc-pinned/node_modules/@anthropic-ai/claude-code/bin/claude.exe auth login`
  (with `CLAUDE_CONFIG_DIR=$PWD/.cc-pinned/config`) → browser → click Authorize.
- Then `python3 tools/eval_l{N}_*.py` (real model, ~10 min each; bg it). Self-auth jail tier is used.
- Clean up between: `pkill -9 -f claude.exe; for s in $(ls /private/tmp/tmux-$(id -u)/|grep eval); do tmux -L $s kill-server; done`.
