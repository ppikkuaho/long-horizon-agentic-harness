# Fork Decisions — build phase (present to user at finished build)

> **Rule (user directive, 2026-06-05):** for a fork encountered during the build, unless it
> **diverges from the spec**: exercise best judgment for the most spec-faithful resolution,
> **note the decision here** (+ a RESOLVED marker in the owning doc), and **proceed — do not
> block**. Present this accumulated list to the user when the build is finished. Stop ONLY on an
> actual blocker (a genuine spec ambiguity/divergence, a test that can't go green, a missing
> dependency). This log is the "present at end" artifact.

| # | Fork | Decision | Rationale | Where recorded |
|---|---|---|---|---|
| FORK-JAIL-TRIGGER | what triggers the per-spawn jail (jail-wiring) | The jail is applied on an **EXPLICIT per-spawn containment REQUEST** (a `level_config.containment` opt-in / a `containment_profile` on the brief), **NOT** on `ledger.RUNTIME_ROOT` being set. | Gating on RUNTIME_ROOT alone would jail the Integration-B (Inc 14) structural spawn, which the frozen test asserts is an UNJAILED bare `env -i` pane — breaking it. The §2.5a design says the containment_profile is "set by the parent/L1 at the chokepoint," i.e. an explicit production-resolution step, not a retroactive change to the structural chokepoint. So the trigger is the explicit request; the structural/dry-run spawn (no request) stays unjailed. | `chokepoint._wants_containment`; jail-wiring; verified (Inc-14 stays green) |
| FORK-CONTAINMENT-CONFIG | the CONFIG dir handed to `resolve_containment` (jail-wiring) | `os.environ["HARNESS_CC_CONFIG_DIR"]` when set (the production/eval seam), else `<RUNTIME_ROOT>/cc-config`. Either is a real on-disk dir the adapter writes the rendered `.sb` into. | A deployment value the daemon binds at boot; the env override is the production seam (and the test's monkeypatch point). **Hardening from the verify gate:** `render_profile` now RE-ALLOWS CONFIG reads (so CC reads its config even when CONFIG sits under READ_DENY_ROOT, the fallback case) while the FINAL last-match token re-deny still closes `.oauth_token` — verified with real sandbox-exec. | `chokepoint._resolve_containment_config_dir` + `sandbox.render_profile`; jail-wiring |
| FORK-PROMOTE | unspecified promotion details (Increment 17) | (a) **the deliverable write path** = a NEW `executor.deliver()` (own-slice, NO legality gate, NO generation bump) — the node is terminal `done` and `is_legal('done','done')` is False, so a lifecycle `transition` can't carry a deliverable-axis stamp; `deliver` writes `deliverable_state`+`delivery_destination` only, `write_targets` untouched (§3.2 distinct fields). (b) **the fence is OPTIONAL** — `_own_slice_write`/`deliver` skip the owner_token fence when `expected_owner_token is None` (the unfenced control-plane promote path; daemon-trusted, §4.3), matching the `watchdog_checkpoint`/`transition` `is not None` precedent; heartbeat/release_lease still pass a real token and stay fenced (verify-gate mutation-confirmed at executor.py:510). (c) **the accept gate is NODE-BOUND** — `accept_signal['node_address'] == node_address` required (an accept for project A must not promote B; a missing/mismatched binding HOLDS the gate — the secure default; hardened in the verify gate). (d) **failure routing** — project-resolution / missing-destination / copy-push faults are ALL caught inside the try → `delivery-failed` + a DISTINCT `delivery_failed_escalation` WAL row (independently assertable from the state-stamp), never an uncaught crash. (e) **git push** = `git push <dest> HEAD:refs/heads/main` in an isolated git env. | Each named-but-unspecified in §3.2 / INTAKE-TO-DELIVERY §3 / the Inc-17 done-test; all spec-faithful. The verify gate FIXED a HIGH (uncaught ValueError), a node-binding gate gap, a None-destination crash, and a too-loose escalation assertion the workflow shipped. | `promote.py` + `executor.deliver` docstrings; built Increment 17 (verify-gate-hardened); row here |
| FORK-CRC | WAL torn/corrupt-record detection (Increment 2) | **crc32 backstop** — `build_wal_record` always emits `crc32(payload)`; `load_wal` treats a crc mismatch as torn alongside the length-prefix mismatch. No `len` field in the json. | The design recommendation; closes the silently-split-record gap the length-prefix alone misses; done-test (e) requires it. (User delegated "you decide" 2026-06-05.) | IMPL-PLAN FORK-CRC RESOLVED marker; built Increment 2 |
| FORK-SEQ | global monotonic `seq` allocation (Increment 2) | **derive from WAL** — `next_seq()` = last WAL `seq` + 1 on load; the WAL is the single source of truth; no authoritative persisted counter. | The design recommendation; no separate counter that can desync from the WAL on a crash. (User delegated "you decide" 2026-06-05.) | IMPL-PLAN FORK-SEQ RESOLVED marker; built Increment 2 |
| FORK-VALIDATE-ERRORSET | what `validate()` errors-on for per-node v1 (Increment 4) | **Three error classes:** (1) illegal lifecycle transition (`is_legal` False) — with the carve-out that `running→running` + `terminal_signal=ESCALATED` is the §3.6 slot-hold (admitted+warned), every OTHER self-loop errors; (2) missing required binding field, using the **minimal CAS-bearing subset** `{node_address, state, generation, owner_token}` (not the full §3.2 schema); (3) `generation != expected_generation+1`. Lesser issues → warnings. | §4.2 + §3.6 pin only the legality + generation-CAS errors as load-bearing and say "lesser → warnings" without enumerating a closed set; §2.7 says port the *discipline*, not the 400-line schema. The minimal required-field set is exactly the CAS preconditions. A later increment may widen the error-set without breaking the contract. | `validate.py` docstring + the §3.6 ESCALATED carve-out (tightened in the verify gate); built Increment 4; row here |
| FORK-VALIDATE-WARNING | the per-node v1 warning *trigger* (Increment 4) | The **ESCALATED slot-hold no-op** (`running→running` with `terminal_signal=ESCALATED`) is admitted (no error) but surfaced as a benign warning. It is the single canonical per-node warning trigger v1 pins; a clean candidate produces **no** warning. | A concrete trigger is needed so warnings-allow is load-bearing (kills "never warns" / "everything-in-errors" mutants); the ESCALATED no-op is the natural one per §3.6 (signal stamped, lifecycle unchanged — admission-worthy-but-benign). | `validate.py`; built Increment 4 |
| FORK-IPC | harnessctl ↔ daemon IPC details (Increment 13) | (a) **handler home** = a new `harnessd/ipc.py` (§3 allows daemon.py or ipc.py). (b) **request/response** = JSON dicts (`{command, addr, expected_*, target_state, event}` → `{ok, binding, errors, ...}`). (c) **socket path** = `<RUNTIME_ROOT>/.harnessd/harnessd.sock` (resolution: `socket_path=` kwarg > `--socket` > `$HARNESSD_SOCKET` > default). (d) **exit codes** = 0 ok / 2 command-abort / 3 transport-failure (no daemon → 3, ledger untouched). (e) **subcommands** = reads `show/next-seq/validate/reconcile-inspect`, mutations `spawn/transition/kill`. (f) **reconcile-inspect is DRY** (replays in-memory, no write). | All named-but-unspecified in §2.x/§4.3; the plan says "a local socket/FIFO", not an exact wire. The CLI-is-not-a-writer invariant is the load-bearing fact (hardened in the verify gate: a dead-socket behavioral guard over EVERY mutation command). | `harnessctl.py`/`ipc.py` docstrings; built Increment 13; row here |
| FORK-PINGER | the launchd job set (Increment 12) | **v1 ships ONE launchd job** (`com.harness.daemon.plist`: KeepAlive=true, RunAtLoad=true, ThrottleInterval≥10). The DAEMON §2.6/§7 design also names a SECOND `harnessd-pinger` job (an external liveness ping) — **DEFERRED**: KeepAlive=true already restarts a crashed daemon, so the pinger is a belt-and-suspenders liveness add-on, not a v1 floor. Named seam for commissioning. | KeepAlive covers the core restart property; the pinger is an enhancement. | `com.harness.daemon.plist`; built Increment 12; row here |
| FORK-WATCHDOG-SEAMS | unspecified watchdog details (Increment 11) | (a) **WatchdogAction** = a tagged action (`kind` ∈ COLLAPSE/NOOP/PROD/FAILED/ESCALATE/WAIT) + node + target + detail. (b) **FORK-PROMPT** = the golden idle-prompt string `prod_precondition` matches (CC-version-specific, KNOWN-OPEN; v1 reads an empty pane = gate CLOSED by default, the conservative no-prod-un-gated posture; commissioning sets the real string). (c) **FORK-LIVENESS-SEAM** = liveness injected via a module-level `set_liveness`/`LIVENESS` seat falling back to `detector.liveness` (the RUNTIME_ROOT/set_adapter precedent). (d) **FORK-WAKE** = `wake_keystroke` returns a pointer ("re-read your .inbox.jsonl, resume"), never the message body. | Each named-but-unspecified in §2.9; all follow the design's intent. The empty-pane-gate-closed default is the safe direction (never prod un-gated). | `watchdog.py` docstrings; built Increment 11; row here |
| FORK-CHOKEPOINT-SEAMS | three unspecified chokepoint details (Increment 10) | (a) **ClaimLost shape** = a not-ok `SpawnResult(ok=False, failure_class="claim_lost")` (a distinct type would also satisfy §2.11; the failure_class shape reuses the existing result type). (b) **L1-escalation transport** = BOTH a `failure_class` on the result AND a `spawn_failed` WAL row naming child-address + the EXACT class that fired (the verify gate hardened this — the Inc-8 auth_expired-vs-model_unavailable distinction must survive into the escalation, not collapse to "some class"). (c) **adapter-injection seam** = a module-level `ADAPTER`/`set_adapter()` (the §2.11 signature carries no adapter param, so it's injected like `ledger.RUNTIME_ROOT`). | Each is named-but-unspecified in §2.11/§6.3; all follow the design's intent (failure_class is the §6.3 classification, the module-injectable mirrors the RUNTIME_ROOT precedent). | `chokepoint.py` docstrings; built Increment 10; row here |
| FORK-VERIFY | how `verify_binary` confirms the pinned binary (Increment 9) | **No-exec in v1:** confirms `config.PINNED_BINARY_VERSION` is set (the pin is the npm version + isolated prefix); the sha256 hash probe is DEFERRED (`config.PINNED_BINARY_HASH` is a v1 placeholder=None) to commissioning. | The dry-run forbids a real `claude --version` exec, and the gitignored install has no captured hash yet; the version-seat check is the load-bearing v1 floor, the hash probe a named commissioning seam. | `claude_code.py` `verify_binary` docstring; built Increment 9 |
| FORK-TRANSCRIPT | the transcript_path derivation (Increment 9) | `<CLAUDE_CONFIG_DIR>/projects/<collapsed-target>/<session_uuid>.jsonl`. The uuid-derived filename is the load-bearing contract (the detector stats it); the exact encoded-cwd path segment is commissioning's to finalize against the real binary. | Claude Code files transcripts under `<config>/projects/<encoded-cwd>/<uuid>.jsonl`; v1 pins the uuid-derived contract (non-null, contains uuid, ends `.jsonl`) and defers the precise cwd-encoding to commissioning. | `claude_code.py` `_transcript_path` docstring; built Increment 9 |
| FORK-TOKEN-EXPIRY | v1 OAuth-token expiry detection (Increment 8) | `check_credential_health` raises `AuthExpired` on an **absent** token; **expiry of a present token is NOT detected in v1** (`_expired()` returns False). | The harness carries `CLAUDE_CODE_OAUTH_TOKEN` as an opaque string with no embedded expiry metadata, and no expiry channel exists yet; the plan's pseudocode names `_expired()` but no data source backs it. `_expired()` is left as the single named seam to wire a future expiry probe (sidecar timestamp / refresh-probe) without touching the absent-token precondition. Absent-token (the genesis-blocking case, DAEMON §7) IS enforced. | `oauth_guard.py` `_expired` docstring; built Increment 8; row here |
| FORK-COORD-DIED-SIGNAL | does a reconcile-killed coordinator stamp `terminal_signal` on its binding? (Increment 7) | **No** — coordinator-died sets `state=dead`, appends the `coordinator_died` run-ledger EVENT, and ESCALATES (the death class lives in the event + the escalation's `kind`), but does **NOT** stamp a binding `terminal_signal`. (Leaf-necro DOES stamp `DIED_INFRA`, which is a valid terminal_signal value.) | The §3.6 `TERMINAL_VOCAB["coordinator_died"]` row pins `terminal_signal=None`; the valid terminal_signal values are DONE\|FAILED\|ESCALATED\|DIED_INFRA\|DIED_METHODOLOGY\|FENCED — "coordinator_died" is an EVENT name, and stamping it as a terminal_signal violates the §3.6 spelling-split. **PROCESS NOTE:** the frozen test asserted `terminal_signal=="coordinator_died"` (diverging from the vocab) and the builder honored it; the verify gate is the authority that reconciles test-vs-spec, so I corrected BOTH the impl and the test to the spec. (A frozen test that diverges from the spec is a *defect in the test*, fixed by the orchestrator — the "frozen" rule binds the builder, not the verify gate.) | `reconcile.py` `_terminal_necro` + `_COORDINATOR_DIED_SIGNAL`; corrected test; built Increment 7 |
| FORK-W-KEYING | how the detector keys the W(state) suspicion window (Increment 6) | The window is keyed off a dedicated **`suspicion_window_key`** binding field (the task-type vocabulary `working`/`waiting_on_child`/`writing_final`, `config.SUSPICION_WINDOWS`), which WATCHDOG §3.3 says the spawning level sets at spawn time — NOT the canonical 4-value `liveness_state` enum (the builder's first cut read `liveness_state`, whose only overlapping token is `working`, silently making the longer windows unreachable). **Deferred wiring:** the chokepoint that POPULATES `suspicion_window_key` lands in Increment 10; until then the field is absent and the detector floors to the tightest `working` (120s) window — errs toward earlier suspicion (safe), never toward masking a stalled node. Reachable the moment the chokepoint sets the field, no detector change needed. | Fixes the verify-gate MEDIUM (an inert spec dimension presented as a benign floor); spec-faithful to "spawning level sets the window at spawn time"; the safe-direction default + named deferral keeps it honest, not silently disabled. Distinct from FORK-W (the window NUMBERS). | `detector.py` `_w_window` docstring; built Increment 6; row here |
| FORK-EXEC-DETAILS | three unspecified executor details (Increment 5) | (a) **lock path** = `RUNTIME_ROOT/.harnessd.lock` (the §2.3/§4.3 single EX serialization lock, co-located with the WAL/binding); (b) **merge-node-into-map** = `commit` reads the whole keyed map fresh via `ledger.all_nodes()` *inside the held EX lock*, splices the candidate at the **validated `node_address`** (the verify gate hardened this: identity + state are set authoritatively from the args, not the delta, so a delta can't re-key the merge), writes the whole map (bystander nodes preserved, no cross-node clobber); (c) **handoff packet** (§4.4 step 3) = a documented **thin stub** in v1 (`_regenerate_handoff_packet`). | Each is named-but-unspecified in §2.6/§4.3/§4.4; all three follow the design's intent directly. The handoff packet is a derived artifact whose v1 content isn't load-bearing for the walking skeleton. | `executor.py` docstrings; built Increment 5; row here |
| FORK-BINDING-FORMAT | binding-ledger on-disk serialization (Increment 2) | **JSON** (not YAML), despite the `binding-ledger.yaml` name. | The design names `.yaml` but nowhere pins the format as load-bearing — the file is daemon-written only (CLIs are clients, §4.3), never human-edited, so YAML's readability isn't needed. JSON is stdlib (no dep), round-trips via `store.normalize_scalars`, and is consistent with the WAL's json framing. Filename centralized in `ledger.BINDING_FILENAME` so a later increment can flip to `.yaml` without touching the interface. (Spec-faithful: faithful to the file's *role*, not the incidental name.) | `ledger.py` docstring; built Increment 2; row here |
