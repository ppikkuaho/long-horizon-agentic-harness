# Bridge — L1-L5 Agent Harness build (2026-06-05)

> Continuity across a compaction. The project: build the **L1-L5 agent harness** — a 5-level LLM-agent system (System Orchestrator → Project Architect → Module Designer → Workstream Coordinator → Task Executor + L5+ reviewer) that builds software from user intent, on a pinned Claude Code. **All work lives in `~/Documents/l1-l5-agent-harness/`** (the session runs from the Life-os cwd, but everything is in the harness repo). **Read first:** `design/INDEX.md` (active-doc map) + `design/working-notes/DEFERRED-REGISTER.md` (every owed/deferred item).

## Where we are: DESIGN COMPLETE → pre-build gap-resolution (then the build)
- **4 runtime cluster specs** done + cross-reconciled: `design/{DAEMON ①, WATCHDOG ②, TRANSPORTS ③, SCALE ④}.md`.
- **Code architecture + 0–16 build-increment queue:** `harnessd/IMPLEMENTATION-PLAN.md`.
- **H40 SOLVED** (in-role boot = `--system-prompt-file`, no binary patch): `H40-RESEARCH-TASK.md` + `research/h40-in-role-boot/`.
- **Pinned vanilla CC 2.1.152**, isolated: `PINNED-CC.md` + `.cc-pinned/` (gitignored).
- **Completeness audit** → homeless concerns triaged into the register; built the **DEFERRED-REGISTER** (the structural cure).
- **Security containment floor DESIGNED + locked** (Option A: `sandbox-exec` seatbelt write/read-jail; skip-perms-in-jail; network/search open, egress deferred; fleet HALT; helper-UID+VM as toggles): `design/SECURITY.md`. Empirical validation (7 gates) deferred to the spawn-chokepoint build.
- **Shared minimal system prompt DRAFTED + approved** (subtractive: 26.8KB→~2KB; one shared file all levels): `working-notes/shared-system-prompt-draft.md`.

## IN FLIGHT (background workflow) — CHECK/AWAIT ITS RESULT
- **Role-docs reconciliation audit**, task **`wz4ecm1pw`** — auditing `operational/` role docs for *agent-facing* staleness vs the current architecture (Phase 0 reconciled them, but BEFORE the cluster designs + the recent reframes). When it returns: present the reconciliation list, then fix the flagged role docs (they are the delivered role bundle — must be current before the role-resolution increment).

## Next (remaining pre-build v1-needs, then the build)
1. **Role-docs reconciliation** — apply the audit's fixes.
2. **Role-resolution increment** — commit the shared system prompt + design the role-bundle delivery (how each level's soul+role+config+handbook + their repo-relative references resolve in the node's *read* context, under the write-jail/read-allowed model). The system prompt is SUBTRACTIVE (mostly remove, minimal add) — user-approved.
3. **Intake→L2 arc + product delivery** (register V3) — the real user-request → running-project → handover path (+ promotion out of the throwaway `/runtime/`).
4. **Build the increments** (IMPLEMENTATION-PLAN 0–16) — each as its OWN build+test workflow.

## How we work (carry these)
- **BUILD via workflows.** Each increment/cluster = its own workflow in a clean context; the orchestrator monitors/integrates/commits (dogfooding the L1-L5 clean-context thesis). **Ultracode is ON** — use Workflow for substantive tasks.
- Workflow shape: **understand → author → adversarial-review → synthesize.** This caught a real defect in *every* cluster. Surface **FORKS** for the user to decide; triage **v1-need vs defer-with-trigger** (the user evaluates each individually; be skeptical of "v1-blocking"; minimal-but-effective, not gold-plated).
- **OAuth/subscription ONLY** — never a raw API key, never `--bare`. All model usage = subscription-window usage.
- **MAINTENANCE RULE:** adding/superseding a doc updates `design/INDEX.md` in the same change; owed/deferred items go in `DEFERRED-REGISTER.md`. Index + register = the **completeness pair** (cure for the homeless/forgotten pattern, which has bitten ~3×).
- **Homeless-concern check:** every concern names the ONE artifact that owns it + what happens across a code-upgrade / project-end-of-life / the human boundary. No line → homeless.
- **Commits:** when the user asks (they've asked per milestone); messages end `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`; commit on `main` (this repo's convention; solo repo, linear history).
- **Absence/completeness audits confabulate** — hand them the corpus / require an `ls`-verify; never trust a map. (The completeness audit falsely declared the specs "missing" because `design/INDEX.md` was stale — now fixed + given a maintenance rule.)

## Key locked decisions (full text in the register's decision subsections + the specs)
- Single-central daemon; one keyed binding-ledger; intent-first WAL; per-seat rows; fencing (`lease_epoch`+`owner_token`+CAS).
- Evidence-lease watchdog (coordinators) + sign-off-or-fail (leaves); detector observed-not-self-reported, inferred-with-bounded-confidence.
- Own bus (per-node `.inbox.jsonl` + tail); user-authorized contact-method registry for L1→user alerts (none active until the user authorizes).
- L5-ephemeral / L1–L4-persistent; **L5 = deliberately-minimal mechanistic code-writer** (bounds the deferred Codex-adapter risk).
- Role delivery: ONE shared minimal system prompt + role-as-delivered-documents.
- States: working/waiting/idle/dead. Naming: Improvement Workspace (not Internal Affairs). Review: dissolved department → per-level review function (#review seat). 5 levels + L5+.

## Recent commit head
`d103103` (SECURITY.md). The system-prompt draft + this bridge are the next commit.
